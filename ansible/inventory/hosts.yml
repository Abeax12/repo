all:
  children:
    certbot:
      hosts:
        web01:
          ansible_host: 203.0.113.10
          ansible_user: ubuntu

    services:
      hosts:
        apache01:
          ansible_host: 203.0.113.20
          ansible_user: ubuntu
        app01:
          ansible_host: 203.0.113.30
          ansible_user: ubuntu
